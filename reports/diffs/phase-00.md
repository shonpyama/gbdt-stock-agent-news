# Phase 00 Diff Summary

- Added repository bootstrap files: `README.md`, `.gitignore`, `pyproject.toml`, `.github/workflows/ci.yml`.
- Added base configuration in `conf/default.yaml`, `conf/cost_model.yaml`, `conf/universe_*.yaml`.
- Initialized package skeleton under `src/gbdt_agent` and test scaffold under `tests/`.
